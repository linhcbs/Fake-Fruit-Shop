import { Product } from "./product.js";
export class fresh extends Product {
    constructor(name, price, category, image, vendor) {
        super(name, price, category, image, vendor);
    }
    //...
}